---
const allPosts = await Astro.glob('../pages/posts/*.md');
---
<h2 class="">Latest Posts</h2>
<ul class="prose prose-invert list-none pl-0">
    {allPosts.slice(-3).map((post) => <li><a href={post.url}>{post.frontmatter.title}</a></li>)}
</ul>